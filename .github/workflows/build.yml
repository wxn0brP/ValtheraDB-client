name: Build

on:
  push:
    branches:
      - master
    tags:
      - "*"

  workflow_dispatch:

concurrency:
  group: build
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: wxn0brP/rusalka@master
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  cdn:
    runs-on: ubuntu-latest
    needs: deploy
    permissions:
      contents: write
      id-token: write

    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - name: Build
        run: |
          bun install
          bun build ./src/valthera.ts --outdir dist --minify --sourcemap
          mv -f package.cdn.json package.json

      - name: Commit to cdn-dist
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin cdn-dist || git checkout --orphan cdn-dist
          git checkout cdn-dist || git checkout --orphan cdn-dist
          git rm -rf .
          git add dist package.json
          git commit -m "Deploy CDN build" || echo "Nothing to commit"
          git push origin cdn-dist --force

      - name: Commit to cdn-dist-tag (if tag)
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          BRANCH_NAME="cdn-dist-$TAG_NAME"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin $BRANCH_NAME || git checkout --orphan $BRANCH_NAME
          git checkout $BRANCH_NAME || git checkout --orphan $BRANCH_NAME
          git rm -rf .
          git add dist package.json
          git commit -m "Deploy CDN build" || echo "Nothing to commit"
          git push origin $BRANCH_NAME --force
